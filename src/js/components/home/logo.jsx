import React from 'react'
import _ from 'lodash'

class Logo extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div className="Logo">
                logo
            </div>
        );
    }
};

// Props
Logo.propTypes = {
};
Logo.defaultProps = {
};

export default Logo;